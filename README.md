# WorkConnect
WorkConnect is a full-stack web application connecting users with local service workers like plumbers, electricians, and carpenters. The frontend is built with React, and the backend uses Spring Boot with JWT security. It supports User, Worker, and Admin roles, worker search with filters, service requests, profile management, and ratings &amp; reviews.
